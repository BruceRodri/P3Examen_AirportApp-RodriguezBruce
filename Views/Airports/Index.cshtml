@model IEnumerable<P3Examen_AirportApp.Models.Airport>

@{
    ViewData["Title"] = "Aeropuertos";
    string searchString = (string)ViewData["SearchString"] ?? "";
    string sortOrder = (string)ViewData["SortOrder"] ?? "";
    int currentPage = (int)ViewData["CurrentPage"];
    int totalPages = (int)ViewData["TotalPages"];
    int totalRecords = (int)ViewData["TotalRecords"];
    int pageSize = (int)ViewData["PageSize"];
}

<h1>Aeropuertos</h1>

<form method="get" asp-action="Index" class="row g-3 mb-3">
    <div class="col-md-4">
        <input type="text" name="searchString" value="@searchString" class="form-control" placeholder="Buscar por nombre, IATA o ICAO..." />
    </div>
    <div class="col-md-2">
        <button type="submit" class="btn btn-primary">Filtrar</button>
    </div>
</form>

<p>
    <a asp-action="Create">Crear nuevo</a>
</p>
<table class="table table-app">
    <thead>
        <tr>
            <th>
                <a asp-action="Index"
                   asp-route-sortOrder="@(sortOrder == "iata" ? "iata_desc" : "iata")"
                   asp-route-searchString="@searchString">@Html.DisplayNameFor(model => model.Iata)</a>
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Icao)
            </th>
            <th>
                <a asp-action="Index"
                   asp-route-sortOrder="@(sortOrder == "name_desc" ? "" : "name_desc")"
                   asp-route-searchString="@searchString">@Html.DisplayNameFor(model => model.Name)</a>
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Iata)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Icao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a class="btn btn-sm btn-outline-primary" asp-action="Edit" asp-route-id="@item.AirportId">Editar</a> <a class="btn btn-sm btn-outline-info" asp-action="Details" asp-route-id="@item.AirportId">Detalles</a> <a class="btn btn-sm btn-outline-danger" asp-action="Delete" asp-route-id="@item.AirportId">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>

<p>
    Pagina @currentPage de @totalPages - @totalRecords registros - Tamano de pagina: @pageSize
</p>

@if (totalPages > 1)
{
    <nav>
        <ul class="pagination">
            <li class="page-item @(currentPage <= 1 ? "disabled" : "")">
                <a class="page-link" asp-action="Index"
                   asp-route-pageNumber="@(currentPage - 1)"
                   asp-route-searchString="@searchString"
                   asp-route-sortOrder="@sortOrder">Anterior</a>
            </li>
            <li class="page-item disabled"><span class="page-link">Pagina @currentPage de @totalPages</span></li>
            <li class="page-item @(currentPage >= totalPages ? "disabled" : "")">
                <a class="page-link" asp-action="Index"
                   asp-route-pageNumber="@(currentPage + 1)"
                   asp-route-searchString="@searchString"
                   asp-route-sortOrder="@sortOrder">Siguiente</a>
            </li>
        </ul>
    </nav>
}
