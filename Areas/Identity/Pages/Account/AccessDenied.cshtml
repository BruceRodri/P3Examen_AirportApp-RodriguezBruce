@page
@model AccessDeniedModel
@{
    ViewData["Title"] = "Acceso denegado";
}

<div class="auth-wrapper">
    <div class="auth-card">
        <div class="card-header">
            <div class="auth-icon"><i class="bi bi-slash-circle-fill"></i></div>
            <div>Acceso denegado</div>
        </div>
        <div class="card-body">
            <p class="text-danger mb-0">No tienes acceso a este recurso.</p>
        </div>
    </div>
</div>
