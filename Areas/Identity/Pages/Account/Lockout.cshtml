@page
@model LockoutModel
@{
    ViewData["Title"] = "Cuenta bloqueada";
}

<div class="auth-wrapper">
    <div class="auth-card">
        <div class="card-header">
            <div class="auth-icon">&#128274;</div>
            <div>Cuenta bloqueada</div>
        </div>
        <div class="card-body">
            <p class="text-danger mb-0">Esta cuenta ha sido bloqueada; inténtalo de nuevo más tarde.</p>
        </div>
    </div>
</div>
